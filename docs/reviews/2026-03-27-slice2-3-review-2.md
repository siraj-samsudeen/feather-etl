# Slice 2-3 Review — Round 2

Created: 2026-03-27
Slice: 2-3 (change detection + incremental extraction)
Prior review: [docs/reviews/2026-03-27-slice2-3-review.md](2026-03-27-slice2-3-review.md)
Fixes plan: `docs/plans/2026-03-27-slice2-3-fixes.md` (referenced by user; file not present in this worktree)
Commit reviewed: 239429e + current uncommitted worktree

---

## Review method

Two-pass re-review:

1. **Execution pass** — ran both required suites before code inspection.
2. **Targeted diff/code pass** — re-checked all prior findings (H-1, H-2, M-1, M-2, L-1) in the current code.

---

## Test results

```bash
uv run pytest -q
  234 passed, 0 failed

bash scripts/hands_on_test.sh
  64 passed, 6 failed
```

Notes:

- The 6 hands-on failures are the same environment-path issue seen earlier (`python -c` using system Python without `duckdb`), not Slice 2/3 logic regressions.
- Incremental checks S-INCR-1..8 pass.

---

## Verdict by prior finding

| ID | Prior finding | Verified status | Evidence |
|---|---|---|---|
| H-1 | Touch-file skip clears incremental `last_value` | ❌ STILL OPEN | `run_table()` skip path still calls `write_watermark()` without `last_value` (`src/feather/pipeline.py:65`); `write_watermark()` still unconditionally sets `last_value = ?` (`src/feather/state.py:157`). |
| H-2 | `overlap_window_minutes` unvalidated (negative risk) | ❌ STILL OPEN | No validation added in `_validate()` (`src/feather/config.py:129`); pipeline still does direct subtraction (`src/feather/pipeline.py:85`). |
| M-1 | Raw SQL composition from config fields | ❌ STILL OPEN | `_build_where_clause()` still interpolates raw `watermark_column`, `watermark_value`, `filter` (`src/feather/sources/file_source.py:45`); source queries still string-formatted in all three source classes. |
| M-2 | State connection leak on exception paths | ❌ STILL OPEN | State methods still rely on normal-path `con.close()` without `finally` guarantees (`src/feather/state.py:32`, `src/feather/state.py:127`, `src/feather/state.py:139`, `src/feather/state.py:171`, `src/feather/state.py:210`). |
| L-1 | Duplicate `write_validation_json()` on `run` path | ❌ STILL OPEN | `cli._load_and_validate()` writes once (`src/feather/cli.py:18`), `pipeline.run_all()` writes again (`src/feather/pipeline.py:201`). |

---

## Additional observations

### N-1 — Referenced fixes plan file is missing from this repo state

- **Severity:** INFO
- **Details:** User referenced `docs/plans/2026-03-27-slice2-3-fixes.md`, but it does not exist in the current worktree (`glob docs/plans/*.md` has no such file).
- **Impact:** Audit chain is incomplete (review → fixes plan link cannot be verified in-repo).

---

## Summary

At the current checked-out state, none of the Slice 2/3 Round-1 findings are fixed yet. Functional behavior remains largely healthy on happy paths, but the original high-severity correctness risks (H-1, H-2) are still present and should be addressed before closing the review.
